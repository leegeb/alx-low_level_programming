#include "main.h"
/**
 * _islower -> checks for lowercase character
 * @c: a character argument
 * Return: returns 1 and 0 based on the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
